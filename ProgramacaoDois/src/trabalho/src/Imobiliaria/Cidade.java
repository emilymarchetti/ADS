package Imobiliaria;

public enum Cidade {

    CIANORTE (Estado.PR),
    MARINGA(Estado.PR),
    JAPURA(Estado.PR),
    LONDRINA(Estado.PR),
    APUCARANA(Estado.PR),
    JUSSARA(Estado.PR),
    UMUARAMA(Estado.PR),
    JACIARA(Estado.MT);

    Cidade(Estado estado) {
    }
}
